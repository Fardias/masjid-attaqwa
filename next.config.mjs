/** @type {import('next').NextConfig} */
// const nextConfig = {
//     output: "standalone",
//     images:{
//         unoptimized: true,
//     }
// };
const nextConfig = {
    // output: "export",
    images:{
        unoptimized: true,
    }
};

export default nextConfig;
